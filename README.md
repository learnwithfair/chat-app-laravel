
-   Run Command:

```bash
composer install
npm install
php artisan migrate
php artisan migrate:fresh --seed
php artisan serve
npm run dev

composer dump-autoload

npm install @tailwindcss/vite --save-dev

```